import React from 'react';

const Hero = () => {
    return (
        <div className='h-screen flex items-center justify-center bg-amber-200'>
            Hero
        </div>
    );
};

export default Hero;